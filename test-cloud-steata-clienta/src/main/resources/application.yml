spring:
  application:
    name: test-cloud-seata-clienta
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        group: test-cloud
        namespace: a91cf009-9642-4c46-bc20-202aebc94f4d
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/seata?serverTimezone=Asia/Shanghai&userUnicode=true&characterEncoding=utf-8&ssl-mode=REQUIRED
    username: ${DB_USER:root}
    password: ${DB_PASSWORD:Qwer0258}
  security:
    oauth2:
      # 资源服务器配置
      resourceserver:
        jwt:
          # Jwt中claims的iss属性，也就是jwt的签发地址，即认证服务器的根路径
          # 资源服务器会进一步的配置，通过该地址获取公钥以解析jwt
          issuer-uri: http://auth.flow.com
